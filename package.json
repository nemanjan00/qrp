{
	"name": "qrp",
	"version": "0.1.0",
	"description": "A data-first, declarative, low-overhead framework for dashboards. Zero RUNTIME dependencies, zero build step — the devDependencies below are only for tests, lint, and git hooks.",
	"type": "module",
	"scripts": {
		"test": "node --test 'test/*.test.js'",
		"lint": "eslint qrp forms browser events toasts proto http utils behaviors collection test",
		"prepare": "husky"
	},
	"lint-staged": {
		"*.js": "eslint --fix"
	},
	"devDependencies": {
		"@babel/core": "^7.29.7",
		"@babel/eslint-parser": "^7.29.7",
		"eslint": "^8.57.1",
		"happy-dom": "^15.11.7",
		"husky": "^9.1.7",
		"lint-staged": "^17.0.8"
	}
}
